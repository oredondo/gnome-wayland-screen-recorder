# Python dependencies for the Zoom & Screen Recorder
# Note: Installing these packages via pip requires system development headers 
# (libdbus-1-dev, libglib2.0-dev, python3-dev). On Debian/Ubuntu/Zorin, we recommend
# using --system-site-packages or installing them via apt:
# sudo apt install python3-dbus python3-gi

dbus-python>=1.2.18
PyGObject>=3.42.0

# Testing, Linting & Security dependencies
pytest>=7.0.0
flake8>=7.0.0
bandit>=1.7.0
